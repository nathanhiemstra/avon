{% include components.elements.forms.radio with {
  'name': name,
  'id': id,
  'theValue': theValue,
  'checked': checked,
  'disabled': disabled,
  'dataAttrs': dataAttrs,
  'theLabel': theLabel,
  'haslabelErrorIcon': haslabelErrorIcon,
  'classes': 'radio-select-combo'
} %}

{# {% include components.elements.forms.select with {
  'id': id,
  'items': items,
  'classes': classes,
  'hasFormGroup': hasFormGroup
} %} #}
