{% if page == 'product-detail' %}
  {% set itemsTotal = '9' %}
  {% set priceTotal = '54.00' %}
  {% set ctaButton = 'Continue' %}

{% elseif page == 'checkout' %}
  {% set itemsTotal = '55' %}
  {% set priceTotal = '484.87' %}
  {% set ctaButton = 'Place Order' %}

{% else %}
  {% set ctaButton = 'Continue' %}
{% endif %}

<!-- DRAWER -->
<div id="drawer-order-total" class="drawer drawer-fixed-bottom drawer-order-summary hidden-md hidden-lg">

  <!-- DRAWER NAV BAR -->
  <section class="navbar navbar-default hidden-md hidden-lg">
    <div class="container pt-4">

      <!-- DRAWER SUMMARY -->
      <div class="pull-left">
        <h4 class="m-0"><a href="#drawer-order-total" data-toggle="drawer"><span class="glyphicon glyphicon-shopping-cart"></span> TOTAL ORDER $456.56</h4>

      </div>
      <div class="pull-right">
        <a href="#drawer-order-total" data-toggle="drawer"><span class="glyphicon glyphicon-chevron-up"></span></a>

        
      </div>
    </div>
  </section>

  <!-- DRAWER CONTENT -->
  <section class="container">

    <p>{{ fpo.sentence }}</p>
    <p>{{ fpo.sentence }}</p>
    <p>{{ fpo.sentence }}</p>
    <p>{{ fpo.sentence }}</p>
    <p>{{ fpo.sentence }}</p>



  </section>
</div><!-- /.drawer -->